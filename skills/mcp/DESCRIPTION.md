---
description: Skills for working with MCP (Model Context Protocol) servers, ACP (Agent Client Protocol) agent providers, tools, and integrations. Includes the built-in native MCP client (configure servers in config.yaml for automatic tool discovery), the mcporter CLI bridge for ad-hoc server interaction, and ACP agent providers for using external coding agents (Claude Code, Codex, Gemini, etc.) as Hermes backends.
---
